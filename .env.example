# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=joker_user
DB_PASSWORD=change_me_in_production
DB_NAME=backend_dev

# MySQL Docker Configuration
MYSQL_ROOT_PASSWORD=change_me_in_production
MYSQL_DATABASE=backend_dev
MYSQL_USER=joker_user
MYSQL_PASSWORD=change_me_in_production

# Application Configuration
ENV=development
LOG_LEVEL=info
PORT=8080

# CORS Configuration
# Comma-separated list of allowed origins (no wildcards allowed)
# Production example: CORS_ALLOWED_ORIGINS=https://example.com,https://app.example.com
# Development example: CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Migration Configuration
MIGRATIONS_PATH=./migrations
