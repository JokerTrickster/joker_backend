# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=joker_user
DB_PASSWORD=change_me_in_production
DB_NAME=backend_dev

# MySQL Docker Configuration
MYSQL_ROOT_PASSWORD=change_me_in_production
MYSQL_DATABASE=backend_dev
MYSQL_USER=joker_user
MYSQL_PASSWORD=change_me_in_production

# Application Configuration
ENV=development
LOG_LEVEL=info
PORT=8080

# CORS Configuration
# Comma-separated list of allowed origins
# Production: Specific domains required (e.g., CORS_ALLOWED_ORIGINS=https://example.com,https://app.example.com)
# Development: Use "*" to allow all origins or leave empty for wildcard default
# Examples:
#   CORS_ALLOWED_ORIGINS=*                                          # Allow all (development only)
#   CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001  # Specific origins
#   CORS_ALLOWED_ORIGINS=https://example.com                        # Production
CORS_ALLOWED_ORIGINS=*

# Migration Configuration
MIGRATIONS_PATH=./migrations
