# Service Configuration
SERVICE_NAME=joker-backend
PORT=6000

# Database Configuration (공유 MySQL, 모든 서비스 동일 DB 사용)
DB_HOST=mysql
DB_PORT=3306
DB_USER=joker_user
DB_PASSWORD=change_this_password
DB_NAME=backend_dev
MYSQL_ROOT_PASSWORD=change_this_root_password

# Application Configuration
LOG_LEVEL=info
ENV=production

# 다른 서비스를 추가하려면 이 파일을 복사하고 포트만 변경하세요
# 예시:
# SERVICE_NAME=game-server
# PORT=6001
# DB_NAME=backend_dev  (모든 서비스가 동일한 데이터베이스 사용)
